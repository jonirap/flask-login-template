# How to use chat REST

### to get the chat 
send a post request with the params:

if its a new chat
* incident_id

if its an old chat
* chat_id

### to send a message
send a post request with:
 * incident_id
 * chat_id
 * user_id
 * message